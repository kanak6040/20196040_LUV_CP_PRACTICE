#include <bits/stdc++.h>
using namespace std;

/* void printbinary(int nums)
{
    for(int i=8 ; i >= 0 ; i--)
        cout << ((nums>>i)&1) ;
} */

int main()
{
     #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif
/* 
    int x=1;
    printbinary(x); */
    /* for(int i= 1; i< 10 ; i++)
    {
        cout<< (x<<i)<< " "<< (x<<i)-1<< endl;
    } */

    /* for(int i= 1; i<=10 ; i++)
    {
        if( i&1 ) cout<<i<<" "<< "odd"<<"\n";
        else cout<<i<<" "<<"Even"<<"\n";
    } */
    /* 
    for(int i= 40; i<=70 ; i++)
    {
        int z= i;
        cout<< z<< " "<< (z>>1)<<" "<< (z<<1) << "\n";    //divide by two
    } */
/*     for(int i=1; i<1e6; i++)
    {
        if(!(i&(i-1))) cout<<i<<" Power of 2 \n";
    } */
    
   /*  int a=5, b=9;
    cout<<a<<" "<<b<<endl;
    a = a^b;
    b = b^a;
    a = a^b;
    cout<<a<<" "<<b<<endl; */
/* 
    unordered_map<int, int> mini;

    for(int i=0; i< 10; i++)
    {
        int x; cin>>x;
        mini[x]++;
    }

    for(auto value:mini)
    {
        if((value.second)&1)
            cout<<value.first<<" ";
    } */

    
}
